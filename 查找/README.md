# 查找

本目录包含二分查找相关示例。二分查找要求待查找数组已经有序。

## 文件说明

| 文件 | 内容 |
| --- | --- |
| `二分查找.c` | 非递归二分查找 |
| `递归二分查找.c` | 递归二分查找 |

## 复习重点

- 二分查找只能直接用于有序表。
- 循环条件通常是 `left <= right`。
- 推荐使用 `mid = left + (right - left) / 2`，避免大整数下标相加溢出。
- 查找失败时通常返回 `-1`。
