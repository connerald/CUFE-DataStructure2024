# 栈

本目录包含顺序栈相关示例。栈是只允许在一端进行插入和删除的线性表，核心操作是入栈、出栈和读取栈顶元素。

## 文件说明

| 文件 | 内容 |
| --- | --- |
| `建立顺序栈.c` | 顺序栈的初始化、入栈、出栈、判空、判满 |
| `3.7_两个栈共享一个数组空间S[m+1].c` | 两个栈共享同一段数组空间 |

## 复习重点

- 栈空条件：通常为 `top == -1`。
- 栈满条件：通常为 `top == MAXSIZE - 1`。
- 入栈前先判满，出栈前先判空。
- 两栈共享空间时，要重点理解两个栈顶指针相向增长的过程。
