# 队列

本目录包含循环队列的基础操作。队列是先进先出的线性结构，循环队列通过取模运算复用数组空间。

## 文件说明

| 文件 | 内容 |
| --- | --- |
| `循环队列相关操作.c` | 循环队列的初始化、入队、出队、判空、判满和遍历 |

## 复习重点

- 队空条件：`front == rear`。
- 常见队满条件：`(rear + 1) % MAX_SIZE == front`。
- 循环队列通常会牺牲一个数组位置来区分队空和队满。
- 入队和出队后都要正确更新 `rear` 或 `front`。
